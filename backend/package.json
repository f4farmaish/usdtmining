{
  "name": "backend",
  "version": "1.0.0",
  "description": "``` mining-app/ ├── backend/ │   ├── server.js       ← Node.js Express backend │   ├── package.json │   └── db.json         ← Auto-created on first run (JSON database) └── frontend/     └── App.jsx         ← React frontend (single file) ```",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "express-rate-limit": "^8.3.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.2.4"
  }
}
